import InTheMaking from '../components/InTheMaking';
export default function ExclusivePage() {
  return (
    <InTheMaking
      pageName="The Vault"
      pageDesc="Exclusive content, members-only sessions, unreleased music, and Inner Circle access. The Vault opens when you're ready."
    />
  );
}
