import InTheMaking from '../components/InTheMaking';
export default function EventsPage() {
  return (
    <InTheMaking
      pageName="Events & Collabs"
      pageDesc="Community events, collaboration drops, tournaments, and live appearances. Stay locked in — the schedule is being forged."
    />
  );
}
